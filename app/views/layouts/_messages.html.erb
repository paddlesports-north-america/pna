
<% flash.each do |name, msg| %>
    <% if msg.is_a?(String) %>
      <%= content_tag :div, msg, :class => "alert #{flash_class( name )} text-center", :id => "flash_#{name}" %>
    <% end %>
<% end %>
